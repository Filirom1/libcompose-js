#!/usr/bin/env node

var fs = require('fs');
var Compose = require('../lib/index').Compose;

var content = fs.readFileSync('docker-compose.yml')
var compose = new Compose().parse(content);

compose.up({}, null, function(err){
  console.error(err);
})
